Javascript is object oriented program and no distinction b/w objects.

While writing functions this is syntax

(function (){
  function function_name(parameters){
   code----;
  }
  call the function--;
})();

Number is a datatype used for both integers and floats.

BigInt is a datype whose range is greater than 2^53-1 and less than -(2^53-1)

Null value wich points to a non-existing object

Undefined is a datatype where value is not assigned

A operator is unary if only one operand is there.
